    {{! header }}
    <main style="text-align: center;">
        <p>Main</p>
        <a href="/auth">Auth</a>
        {{#if dados}}
            <p>Olá, {{dados.username}}</p>
        {{/if}}
    </main>